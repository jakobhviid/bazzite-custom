---
name: Build container image
on:
  pull_request:
    branches:
      - main
  schedule:
    # Every 3 hours, on the half-hour. Most invocations exit in <30s — the
    # `Check if base image changed` step compares the upstream Bazzite
    # `:stable` digest to the digest stored as a label on our last published
    # image, and skips the rest of the workflow when nothing has changed.
    # When Bazzite publishes a new stable, the next 3-hour tick picks it up.
    - cron: '30 */3 * * *'
  push:
    branches:
      - main
    paths-ignore:
      - '**/README.md'
  workflow_dispatch:

env:
  IMAGE_DESC: "Jakob's customized Bazzite image (browsers + 1Password + GNOME extensions baked in)"
  IMAGE_KEYWORDS: "bootc,ublue,bazzite,gnome"
  IMAGE_LOGO_URL: "https://avatars.githubusercontent.com/u/120078124?s=200&v=4"
  IMAGE_REGISTRY: "ghcr.io/${{ github.repository_owner }}"
  DEFAULT_TAG: "latest"
  # Custom OCI label key for storing the upstream base image digest. Read back
  # by the `check_base` step on subsequent runs to gate scheduled builds.
  BASE_DIGEST_LABEL: "phd.hviid.bazzite-custom.base-digest"

jobs:
  build_push:
    name: Build ${{ matrix.image_name }}
    runs-on: ubuntu-24.04

    # Per-variant concurrency — let both matrix legs run, but cancel stale
    # in-progress runs of the same variant on a new push. Lives at job level
    # because workflow-level concurrency can't see the matrix context.
    concurrency:
      group: ${{ github.workflow }}-${{ matrix.image_name }}-${{ github.ref || github.run_id }}
      cancel-in-progress: true

    strategy:
      fail-fast: false
      matrix:
        include:
          # Intel-only laptop (X1 Carbon)
          - base_image: bazzite-gnome
            image_name: bazzite-custom
          # NVIDIA desktops (Atlas + Annika) — open kernel module required for RTX 50-series
          - base_image: bazzite-gnome-nvidia-open
            image_name: bazzite-nvidia-custom

    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - name: Prepare environment
        run: |
          IMAGE_REGISTRY="${{ env.IMAGE_REGISTRY }}"
          IMAGE_NAME="${{ matrix.image_name }}"
          echo "IMAGE_REGISTRY=${IMAGE_REGISTRY,,}" >> ${GITHUB_ENV}
          echo "IMAGE_NAME=${IMAGE_NAME,,}" >> ${GITHUB_ENV}

      # Compare the upstream Bazzite base digest to the digest stored as a
      # label on our last published image. Skip the rest of the workflow on
      # scheduled runs when they match (fast — single skopeo inspect each).
      # Pushes and manual dispatches always proceed: they mean *we* changed
      # something on our end and want a fresh build regardless of upstream.
      - name: Check if base image changed
        id: check_base
        run: |
          BASE="ghcr.io/ublue-os/${{ matrix.base_image }}:stable"
          OURS="${IMAGE_REGISTRY}/${{ matrix.image_name }}:latest"

          # Authenticate against GHCR so we can inspect our own image (which
          # may be private). Tolerate failure — pull_request events from
          # forks have read-only tokens and may not login; inspecting the
          # upstream public Bazzite image works anonymously, and a failed
          # inspect of ours just yields LAST="" → we proceed to build.
          echo "${{ secrets.GITHUB_TOKEN }}" | \
            skopeo login ghcr.io --username "${{ github.actor }}" --password-stdin \
            || echo "skopeo login failed (probably PR token); continuing anonymously"

          # Upstream is public, never needs auth.
          NEW=$(skopeo inspect "docker://$BASE" --format '{{.Digest}}')
          # If our image doesn't exist yet (first build) or has no digest
          # label (built by an older workflow), LAST is empty -> we build.
          LAST=$(skopeo inspect "docker://$OURS" --format "{{ index .Labels \"${BASE_DIGEST_LABEL}\" }}" 2>/dev/null || echo "")

          echo "Upstream $BASE digest: $NEW"
          echo "Our last-built-against digest: ${LAST:-<none>}"

          echo "new=$NEW"   >> "$GITHUB_OUTPUT"
          echo "last=$LAST" >> "$GITHUB_OUTPUT"

          if [[ "$NEW" == "$LAST" && "${{ github.event_name }}" == "schedule" ]]; then
            echo "Base unchanged on a scheduled run — skipping build."
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "Building (event=${{ github.event_name }}, base $LAST -> $NEW)."
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout
        if: steps.check_base.outputs.skip != 'true'
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Maximize build space
        if: steps.check_base.outputs.skip != 'true'
        uses: ublue-os/remove-unwanted-software@695eb75bc387dbcd9685a8e72d23439d8686cba6

      - name: Get current date
        if: steps.check_base.outputs.skip != 'true'
        id: date
        run: |
          echo "date=$(date -u +%Y\-%m\-%d\T%H\:%M\:%S\Z)" >> $GITHUB_OUTPUT

      - name: Image Metadata
        if: steps.check_base.outputs.skip != 'true'
        uses: docker/metadata-action@030e881283bb7a6894de51c315a6bfe6a94e05cf # v6.0.0
        id: metadata
        with:
          tags: |
            type=raw,value=${{ env.DEFAULT_TAG }}
            type=raw,value=${{ env.DEFAULT_TAG }}.{{date 'YYYYMMDD'}}
            type=raw,value={{date 'YYYYMMDD'}}
            type=sha,enable=${{ github.event_name == 'pull_request' }}
            type=ref,event=pr
          labels: |
            io.artifacthub.package.readme-url=https://raw.githubusercontent.com/${{ github.repository_owner }}/${{ github.event.repository.name }}/${{ github.sha }}/README.md
            org.opencontainers.image.created=${{ steps.date.outputs.date }}
            org.opencontainers.image.description=${{ env.IMAGE_DESC }}
            org.opencontainers.image.documentation=https://raw.githubusercontent.com/${{ github.repository_owner }}/${{ github.event.repository.name }}/${{ github.sha }}/README.md
            org.opencontainers.image.source=https://github.com/${{ github.repository_owner }}/${{ github.event.repository.name }}/blob/${{ github.sha }}/Containerfile
            org.opencontainers.image.title=${{ env.IMAGE_NAME }}
            org.opencontainers.image.url=https://github.com/${{ github.repository_owner }}/${{ github.event.repository.name }}/tree/${{ github.sha }}
            org.opencontainers.image.vendor=${{ github.repository_owner }}
            org.opencontainers.image.version=${{ env.DEFAULT_TAG }}.{{date 'YYYYMMDD'}}
            io.artifacthub.package.deprecated=false
            io.artifacthub.package.keywords=${{ env.IMAGE_KEYWORDS }}
            io.artifacthub.package.license=Apache-2.0
            io.artifacthub.package.logo-url=${{ env.IMAGE_LOGO_URL }}
            io.artifacthub.package.prerelease=false
            containers.bootc=1
            ${{ env.BASE_DIGEST_LABEL }}=${{ steps.check_base.outputs.new }}
          sep-tags: " "
          sep-annotations: " "

      - name: Build Image
        if: steps.check_base.outputs.skip != 'true'
        id: build_image
        uses: redhat-actions/buildah-build@7a95fa7ee0f02d552a32753e7414641a04307056 # v2
        with:
          containerfiles: |
            ./Containerfile
          image: ${{ env.IMAGE_NAME }}
          tags: ${{ steps.metadata.outputs.tags }}
          labels: ${{ steps.metadata.outputs.labels }}
          oci: false
          build-args: |
            BASE_IMAGE=${{ matrix.base_image }}

      # Rechunk: split the single big custom layer into ~127 stable,
      # RPM-ownership-grouped chunks so update pulls only re-download chunks
      # whose RPMs actually changed. Typical weekly Bazzite-base bump: ~50-200
      # MB delta instead of ~900 MB-1 GB. Uses native `rpm-ostree compose
      # build-chunked-oci` from the just-built image itself — no external
      # rechunk container or action needed (Bazzite's own current pattern).
      - name: Rechunk image
        if: steps.check_base.outputs.skip != 'true'
        shell: bash
        run: |
          set -euo pipefail
          IMAGE="${IMAGE_NAME}"
          TAGS="${{ steps.metadata.outputs.tags }}"
          FIRST_TAG="$(echo "$TAGS" | awk '{print $1}')"

          # buildah-build@v2 writes to rootless storage; chunking needs rootful
          # podman --privileged. Bridge the two storages via oci-archive.
          buildah push "${IMAGE}:${FIRST_TAG}" "oci-archive:/tmp/${IMAGE}.tar"
          sudo skopeo copy "oci-archive:/tmp/${IMAGE}.tar" \
            "containers-storage:localhost/${IMAGE}:${FIRST_TAG}"
          rm -f "/tmp/${IMAGE}.tar"

          # Pre-chunk hygiene: drop /run and /tmp cruft from the rootfs so it
          # doesn't end up in a chunk (Bazzite's published pattern).
          CTR=$(sudo buildah from "localhost/${IMAGE}:${FIRST_TAG}")
          MNT=$(sudo buildah mount "$CTR")
          sudo bash -c "rm -rf $MNT/run/.* $MNT/run/* $MNT/tmp/.* $MNT/tmp/* 2>/dev/null || true"
          sudo buildah umount "$CTR"
          sudo buildah commit --identity-label=false --rm "$CTR" "localhost/${IMAGE}:${FIRST_TAG}"

          # Chunk: stable RPM-ownership-grouped layers. --max-layers 127 stays
          # under the OCI 128-layer ceiling. --format-version 2 is the current
          # bootc-compatible output. Runs inside the image itself — its own
          # rpm-ostree walks the rootfs.
          sudo podman run --rm --privileged \
            --volume /var/lib/containers:/var/lib/containers \
            "localhost/${IMAGE}:${FIRST_TAG}" \
            rpm-ostree compose build-chunked-oci \
              --bootc \
              --max-layers 127 \
              --format-version 2 \
              --from "containers-storage:localhost/${IMAGE}:${FIRST_TAG}" \
              --output "containers-storage:localhost/${IMAGE}:chunked"

          # Replace every tag the metadata step produced with the chunked
          # image, in rootful storage.
          for TAG in $TAGS; do
            sudo podman tag "localhost/${IMAGE}:chunked" "localhost/${IMAGE}:${TAG}"
          done

          # Copy the chunked tags back into rootless storage so the existing
          # push-to-registry@v2 step (which runs rootless buildah) finds them.
          ROOTLESS_STORAGE="${XDG_DATA_HOME:-$HOME/.local/share}/containers/storage"
          for TAG in $TAGS; do
            sudo skopeo copy \
              "containers-storage:localhost/${IMAGE}:${TAG}" \
              "containers-storage:[overlay@${ROOTLESS_STORAGE}+${ROOTLESS_STORAGE}]localhost/${IMAGE}:${TAG}"
          done
          # Hand ownership back to the runner user — sudo skopeo wrote as root.
          sudo chown -R "$(id -u):$(id -g)" "${ROOTLESS_STORAGE}"

      - name: Login to GitHub Container Registry
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121 # v4.1.0
        if: steps.check_base.outputs.skip != 'true' && github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push To GHCR
        uses: redhat-actions/push-to-registry@5ed88d269cf581ea9ef6dd6806d01562096bee9c # v2
        if: steps.check_base.outputs.skip != 'true' && github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        id: push
        env:
          REGISTRY_USER: ${{ github.actor }}
          REGISTRY_PASSWORD: ${{ github.token }}
        with:
          registry: ${{ env.IMAGE_REGISTRY }}
          image: ${{ env.IMAGE_NAME }}
          tags: ${{ steps.metadata.outputs.tags }}
          username: ${{ env.REGISTRY_USER }}
          password: ${{ env.REGISTRY_PASSWORD }}

      - name: Install Cosign
        uses: sigstore/cosign-installer@faadad0cce49287aee09b3a48701e75088a2c6ad # v4.0.0
        if: steps.check_base.outputs.skip != 'true' && github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        with:
          cosign-release: 'v2.6.1'

      # Sign by digest (immutable manifest reference) rather than by each
      # tag. The signature is bound to the bytes that were pushed; subsequent
      # tag-moves (which we don't do, but could) wouldn't accidentally
      # invalidate the trust path. Single cosign call replaces the previous
      # per-tag loop and silences the "uses a tag, not a digest" warning.
      - name: Sign container image
        if: steps.check_base.outputs.skip != 'true' && github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        run: |
          IMAGE_FULL="${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}"
          DIGEST="${{ steps.push.outputs.digest }}"
          cosign sign -y --key env://COSIGN_PRIVATE_KEY "${IMAGE_FULL}@${DIGEST}"
        env:
          COSIGN_EXPERIMENTAL: false
          COSIGN_PRIVATE_KEY: ${{ secrets.SIGNING_SECRET }}
          COSIGN_PASSWORD: ${{ secrets.COSIGN_PASSWORD }}
